//import CardGenre from "./CardGenre";
//import { genres } from "@/data/genres";

export default function GroupCardGenre() {
    return (
        // <div className="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4 mt-12">
        //     {genres.map((genre) => (
        //         <CardGenre 
        //             key={genre.id} 
        //             url={`/genre/${genre.id}`} 
        //             cover={genre.cover} 
        //         />
        //     ))}
        // </div>
        <></>
    );
}
